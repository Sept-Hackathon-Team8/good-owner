import React from "react";

const AddPet = () => {
  return (
    <div className="add-pet-container">
      <p>probably using a form here but I have a button for now</p>
      <button>Add Pet</button>
    </div>
  );
};

export default AddPet;